import ListTeamModule from '@/modules/list-team';

import { AppPage } from '@/types/app';

const ListTeam: AppPage = () => {
	return <ListTeamModule />;
};

ListTeam.Layout = 'Default';

export default ListTeam;
